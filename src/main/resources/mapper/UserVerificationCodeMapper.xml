<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qyt.qytbackend.mapper.UserVerificationCodeMapper">
    <!-- 结果映射 -->
    <resultMap id="UserVerificationCodeResultMap" type="UserVerificationCode">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="phone_number" property="phoneNumber" jdbcType="VARCHAR" />
        <result column="verification_code" property="verificationCode" jdbcType="VARCHAR" />
        <result column="create_pin" property="createPin" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_pin" property="updatePin" jdbcType="VARCHAR" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
        <result column="is_delete" property="isDelete" jdbcType="TINYINT" />
    </resultMap>

    <!-- 插入语句 -->
    <insert id="insert" parameterType="UserVerificationCode" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_verification_code (
            phone_number,
            verification_code,
            create_pin,
            create_time,
            update_pin,
            update_time,
            is_delete
        ) VALUES (
            #{phoneNumber,jdbcType=VARCHAR},
            #{verificationCode,jdbcType=VARCHAR},
            #{createPin,jdbcType=VARCHAR},
            #{createTime,jdbcType=TIMESTAMP},
            #{updatePin,jdbcType=VARCHAR},
            #{updateTime,jdbcType=TIMESTAMP},
            #{isDelete,jdbcType=TINYINT}
        )
    </insert>

    <!-- 删除语句 -->
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM user_verification_code WHERE id = #{id,jdbcType=BIGINT}
    </delete>

    <!-- 更新语句 -->
    <update id="update" parameterType="UserVerificationCode">
        UPDATE user_verification_code
        <set>
            <if test="phoneNumber != null">phone_number = #{phoneNumber,jdbcType=VARCHAR},</if>
            <if test="verificationCode != null">verification_code = #{verificationCode,jdbcType=VARCHAR},</if>
            <if test="createPin != null">create_pin = #{createPin,jdbcType=VARCHAR},</if>
            <if test="createTime != null">create_time = #{createTime,jdbcType=TIMESTAMP},</if>
            <if test="updatePin != null">update_pin = #{updatePin,jdbcType=VARCHAR},</if>
            <if test="updateTime != null">update_time = #{updateTime,jdbcType=TIMESTAMP},</if>
            <if test="isDelete != null">is_delete = #{isDelete,jdbcType=TINYINT},</if>
        </set>
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

    <!-- 根据ID查询 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="UserVerificationCodeResultMap">
        SELECT * FROM user_verification_code WHERE id = #{id,jdbcType=BIGINT} AND is_delete = 0
    </select>

    <!-- 根据手机号查询 -->
    <select id="selectByPhoneNumber" parameterType="java.lang.String" resultMap="UserVerificationCodeResultMap">
        SELECT * FROM user_verification_code WHERE phone_number = #{phoneNumber,jdbcType=VARCHAR} AND is_delete = 0
    </select>

    <!-- 查询所有 -->
    <select id="selectAll" resultMap="UserVerificationCodeResultMap">
        SELECT * FROM user_verification_code WHERE is_delete = 0
    </select>
</mapper>