<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qyt.qytbackend.mapper.ShoppingCartMapper">
    <!-- 结果映射 -->
    <resultMap id="ShoppingCartResultMap" type="ShoppingCart">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="user_id" property="userId" jdbcType="INTEGER" />
        <result column="product_id" property="productId" jdbcType="INTEGER" />
        <result column="product_sku" property="productSku" jdbcType="VARCHAR" />
        <result column="quantity" property="quantity" jdbcType="INTEGER" />
        <result column="selected" property="selected" jdbcType="TINYINT" />
        <result column="created_time" property="createdTime" jdbcType="TIMESTAMP" />
        <result column="updated_time" property="updatedTime" jdbcType="TIMESTAMP" />
        <result column="create_pin" property="createPin" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_pin" property="updatePin" jdbcType="VARCHAR" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
        <result column="is_delete" property="isDelete" jdbcType="TINYINT" />
    </resultMap>

    <!-- 插入语句 -->
    <insert id="insert" parameterType="ShoppingCart" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO shopping_cart (
            user_id,
            product_id,
            product_sku,
            quantity,
            selected,
            created_time,
            updated_time,
            create_pin,
            create_time,
            update_pin,
            update_time,
            is_delete
        ) VALUES (
            #{userId,jdbcType=INTEGER},
            #{productId,jdbcType=INTEGER},
            #{productSku,jdbcType=VARCHAR},
            #{quantity,jdbcType=INTEGER},
            #{selected,jdbcType=TINYINT},
            #{createdTime,jdbcType=TIMESTAMP},
            #{updatedTime,jdbcType=TIMESTAMP},
            #{createPin,jdbcType=VARCHAR},
            #{createTime,jdbcType=TIMESTAMP},
            #{updatePin,jdbcType=VARCHAR},
            #{updateTime,jdbcType=TIMESTAMP},
            #{isDelete,jdbcType=TINYINT}
        )
    </insert>

    <!-- 删除语句 -->
    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM shopping_cart WHERE id = #{id,jdbcType=INTEGER}
    </delete>

    <!-- 更新语句 -->
    <update id="update" parameterType="ShoppingCart">
        UPDATE shopping_cart
        <set>
            <if test="userId != null">user_id = #{userId,jdbcType=INTEGER},</if>
            <if test="productId != null">product_id = #{productId,jdbcType=INTEGER},</if>
            <if test="productSku != null">product_sku = #{productSku,jdbcType=VARCHAR},</if>
            <if test="quantity != null">quantity = #{quantity,jdbcType=INTEGER},</if>
            <if test="selected != null">selected = #{selected,jdbcType=TINYINT},</if>
            <if test="createdTime != null">created_time = #{createdTime,jdbcType=TIMESTAMP},</if>
            <if test="updatedTime != null">updated_time = #{updatedTime,jdbcType=TIMESTAMP},</if>
            <if test="createPin != null">create_pin = #{createPin,jdbcType=VARCHAR},</if>
            <if test="createTime != null">create_time = #{createTime,jdbcType=TIMESTAMP},</if>
            <if test="updatePin != null">update_pin = #{updatePin,jdbcType=VARCHAR},</if>
            <if test="updateTime != null">update_time = #{updateTime,jdbcType=TIMESTAMP},</if>
            <if test="isDelete != null">is_delete = #{isDelete,jdbcType=TINYINT},</if>
        </set>
        WHERE id = #{id,jdbcType=INTEGER}
    </update>

    <!-- 根据ID查询 -->
    <select id="selectById" parameterType="java.lang.Integer" resultMap="ShoppingCartResultMap">
        SELECT * FROM shopping_cart WHERE id = #{id,jdbcType=INTEGER}
    </select>

    <!-- 根据用户ID查询 -->
    <select id="selectByUserId" parameterType="java.lang.Integer" resultMap="ShoppingCartResultMap">
        SELECT * FROM shopping_cart WHERE user_id = #{userId,jdbcType=INTEGER}
    </select>

    <!-- 根据商品ID查询 -->
    <select id="selectByProductId" parameterType="java.lang.Integer" resultMap="ShoppingCartResultMap">
        SELECT * FROM shopping_cart WHERE product_id = #{productId,jdbcType=INTEGER}
    </select>

    <!-- 根据是否选中查询 -->
    <select id="selectBySelected" parameterType="java.lang.Integer" resultMap="ShoppingCartResultMap">
        SELECT * FROM shopping_cart WHERE selected = #{selected,jdbcType=INTEGER}
    </select>

    <!-- 查询所有 -->
    <select id="selectAll" resultMap="ShoppingCartResultMap">
        SELECT * FROM shopping_cart
    </select>
</mapper>