<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qyt.qytbackend.mapper.ProductBrandMapper">
    <!-- 结果映射 -->
    <resultMap id="ProductBrandResultMap" type="ProductBrand">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="brand_name" property="brandName" jdbcType="VARCHAR" />
        <result column="brand_logo" property="brandLogo" jdbcType="VARCHAR" />
        <result column="brand_desc" property="brandDesc" jdbcType="VARCHAR" />
        <result column="status" property="status" jdbcType="TINYINT" />
        <result column="create_pin" property="createPin" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_pin" property="updatePin" jdbcType="VARCHAR" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
        <result column="is_delete" property="isDelete" jdbcType="TINYINT" />
    </resultMap>

    <!-- 插入语句 -->
    <insert id="insert" parameterType="ProductBrand" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_brand (
            brand_name,
            brand_logo,
            brand_desc,
            status,
            create_pin,
            create_time,
            update_pin,
            update_time,
            is_delete
        ) VALUES (
            #{brandName,jdbcType=VARCHAR},
            #{brandLogo,jdbcType=VARCHAR},
            #{brandDesc,jdbcType=VARCHAR},
            #{status,jdbcType=TINYINT},
            #{createPin,jdbcType=VARCHAR},
            #{createTime,jdbcType=TIMESTAMP},
            #{updatePin,jdbcType=VARCHAR},
            #{updateTime,jdbcType=TIMESTAMP},
            #{isDelete,jdbcType=TINYINT}
        )
    </insert>

    <!-- 删除语句 -->
    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM product_brand WHERE id = #{id,jdbcType=INTEGER}
    </delete>

    <!-- 更新语句 -->
    <update id="update" parameterType="ProductBrand">
        UPDATE product_brand
        <set>
            <if test="brandName != null">brand_name = #{brandName,jdbcType=VARCHAR},</if>
            <if test="brandLogo != null">brand_logo = #{brandLogo,jdbcType=VARCHAR},</if>
            <if test="brandDesc != null">brand_desc = #{brandDesc,jdbcType=VARCHAR},</if>
            <if test="status != null">status = #{status,jdbcType=TINYINT},</if>
            <if test="createPin != null">create_pin = #{createPin,jdbcType=VARCHAR},</if>
            <if test="createTime != null">create_time = #{createTime,jdbcType=TIMESTAMP},</if>
            <if test="updatePin != null">update_pin = #{updatePin,jdbcType=VARCHAR},</if>
            <if test="updateTime != null">update_time = #{updateTime,jdbcType=TIMESTAMP},</if>
            <if test="isDelete != null">is_delete = #{isDelete,jdbcType=TINYINT},</if>
        </set>
        WHERE id = #{id,jdbcType=INTEGER}
    </update>

    <!-- 根据ID查询 -->
    <select id="selectById" parameterType="java.lang.Integer" resultMap="ProductBrandResultMap">
        SELECT * FROM product_brand WHERE id = #{id,jdbcType=INTEGER}
    </select>

    <!-- 根据品牌名称查询 -->
    <select id="selectByBrandName" parameterType="java.lang.String" resultMap="ProductBrandResultMap">
        SELECT * FROM product_brand WHERE brand_name = #{brandName,jdbcType=VARCHAR}
    </select>

    <!-- 根据状态查询 -->
    <select id="selectByStatus" parameterType="java.lang.Integer" resultMap="ProductBrandResultMap">
        SELECT * FROM product_brand WHERE status = #{status,jdbcType=INTEGER}
    </select>

    <!-- 查询所有 -->
    <select id="selectAll" resultMap="ProductBrandResultMap">
        SELECT * FROM product_brand
    </select>
</mapper>