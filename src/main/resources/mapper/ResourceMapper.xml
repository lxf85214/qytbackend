<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qyt.qytbackend.mapper.ResourceMapper">
    <!-- 结果映射 -->
    <resultMap id="ResourceResultMap" type="Resource">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="pid" property="pid" jdbcType="INTEGER" />
        <result column="type" property="type" jdbcType="VARCHAR" />
        <result column="url" property="url" jdbcType="VARCHAR" />
        <result column="level" property="level" jdbcType="INTEGER" />
        <result column="order_num" property="orderNum" jdbcType="INTEGER" />
        <result column="icon" property="icon" jdbcType="VARCHAR" />
        <result column="status" property="status" jdbcType="TINYINT" />
        <result column="create_pin" property="createPin" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_pin" property="updatePin" jdbcType="VARCHAR" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
        <result column="is_delete" property="isDelete" jdbcType="TINYINT" />
    </resultMap>

    <!-- 插入语句 -->
    <insert id="insert" parameterType="Resource" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO resource (
            name,
            pid,
            type,
            url,
            level,
            order_num,
            icon,
            status,
            create_pin,
            create_time,
            update_pin,
            update_time,
            is_delete
        ) VALUES (
            #{name,jdbcType=VARCHAR},
            #{pid,jdbcType=INTEGER},
            #{type,jdbcType=VARCHAR},
            #{url,jdbcType=VARCHAR},
            #{level,jdbcType=INTEGER},
            #{orderNum,jdbcType=INTEGER},
            #{icon,jdbcType=VARCHAR},
            #{status,jdbcType=TINYINT},
            #{createPin,jdbcType=VARCHAR},
            #{createTime,jdbcType=TIMESTAMP},
            #{updatePin,jdbcType=VARCHAR},
            #{updateTime,jdbcType=TIMESTAMP},
            #{isDelete,jdbcType=TINYINT}
        )
    </insert>

    <!-- 删除语句 -->
    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM resource WHERE id = #{id,jdbcType=INTEGER}
    </delete>

    <!-- 更新语句 -->
    <update id="update" parameterType="Resource">
        UPDATE resource
        <set>
            <if test="name != null">name = #{name,jdbcType=VARCHAR},</if>
            <if test="pid != null">pid = #{pid,jdbcType=INTEGER},</if>
            <if test="type != null">type = #{type,jdbcType=VARCHAR},</if>
            <if test="url != null">url = #{url,jdbcType=VARCHAR},</if>
            <if test="level != null">level = #{level,jdbcType=INTEGER},</if>
            <if test="orderNum != null">order_num = #{orderNum,jdbcType=INTEGER},</if>
            <if test="icon != null">icon = #{icon,jdbcType=VARCHAR},</if>
            <if test="status != null">status = #{status,jdbcType=TINYINT},</if>
            <if test="createPin != null">create_pin = #{createPin,jdbcType=VARCHAR},</if>
            <if test="createTime != null">create_time = #{createTime,jdbcType=TIMESTAMP},</if>
            <if test="updatePin != null">update_pin = #{updatePin,jdbcType=VARCHAR},</if>
            <if test="updateTime != null">update_time = #{updateTime,jdbcType=TIMESTAMP},</if>
            <if test="isDelete != null">is_delete = #{isDelete,jdbcType=TINYINT},</if>
        </set>
        WHERE id = #{id,jdbcType=INTEGER}
    </update>

    <!-- 根据ID查询 -->
    <select id="selectById" parameterType="java.lang.Integer" resultMap="ResourceResultMap">
        SELECT * FROM resource WHERE id = #{id,jdbcType=INTEGER}
    </select>

    <!-- 根据父ID查询 -->
    <select id="selectByPid" parameterType="java.lang.Integer" resultMap="ResourceResultMap">
        SELECT * FROM resource WHERE pid = #{pid,jdbcType=INTEGER}
    </select>

    <!-- 根据类型查询 -->
    <select id="selectByType" parameterType="java.lang.String" resultMap="ResourceResultMap">
        SELECT * FROM resource WHERE type = #{type,jdbcType=VARCHAR}
    </select>

    <!-- 根据状态查询 -->
    <select id="selectByStatus" parameterType="java.lang.Integer" resultMap="ResourceResultMap">
        SELECT * FROM resource WHERE status = #{status,jdbcType=INTEGER}
    </select>

    <!-- 查询所有 -->
    <select id="selectAll" resultMap="ResourceResultMap">
        SELECT * FROM resource
    </select>
</mapper>